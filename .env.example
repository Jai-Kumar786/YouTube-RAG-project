# PostgreSQL connection
# Replace <username> and <password> with your PostgreSQL credentials.
# For local development with docker-compose, refer to the credentials in docker-compose.yml
DATABASE_URL=postgresql://<username>:<password>@localhost:5433/youtube_rag

# Ollama base URL (default for local install)
OLLAMA_BASE_URL=http://localhost:11434

# LLM model to use for generation (Ollama cloud model)
LLM_MODEL=deepseek3.1:671b-cloud
